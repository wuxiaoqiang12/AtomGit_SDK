{
  "_comment": "Example configuration for atomgit-sdk. Copy to config.json and fill in your values. The 'token' field accepts the literal '$ATOMGIT_TOKEN' placeholder which is resolved from the environment variable of the same name at runtime.",
  "atomgit": {
    "token": "$ATOMGIT_TOKEN",
    "owner": "your-org",
    "repo": "your-repo",
    "baseUrl": "https://api.atomgit.com"
  }
}
